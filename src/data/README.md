# Generated Data

`players.generated.json` and `puzzles.generated.json` are generated from the current prototype in `index.html`.

Run:

```bash
npm run generate:data
```

These files are a temporary Phase 1 bridge. The long-term source of truth should become normalized football data plus authored/generated puzzle definitions.
